# .ai-context

โครงสร้างย่อของระบบ (อัพเดตเมื่อมีการเปลี่ยนแปลงสำคัญ)

## Stack
- Node.js + Express + EJS
- Bootstrap 5 + Bootstrap Icons (CDN)
- ใช้ mock data ใน route ตรง ๆ ยังไม่มี database

## Routes / Modules
- `/`              — แดชบอร์ด (`routes/index.js`)
- `/loans`         — รายการสินเชื่อ + ยื่นคำขอ + รายละเอียด (`routes/loans.js`)
- `/payments`      — การชำระเงิน (`routes/payments.js`)
- `/customers`     — ข้อมูลลูกค้า (`routes/customers.js`)
- `/collaterals`   — หลักประกัน CRUD (`routes/collaterals.js`)
  - 1 หลักประกัน : 1 สัญญา (สถานะ: ว่าง / ใช้ค้ำอยู่ / ปลดแล้ว)
  - ผูกกับ loan ตอนทำสัญญา ไม่ผูกกับ customer โดยตรง
- `/reports`       — ศูนย์รายงาน (`routes/reports.js`)
  - sub-routes: `/summary`, `/revenue`, `/overdue`, `/portfolio`, `/collateral`, `/customer`
  - mock data ทั้งหมดอยู่ในไฟล์ route, charts ใช้ progress bar / SVG donut (no Chart.js)
- `/loans?status=all|pending|approved|rejected|closed` — ฟิลเตอร์ตามสถานะผ่าน query param
- `/settings`     — ตั้งค่าระบบ (`routes/settings.js`) — redirect ไป `/settings/general`
  - sub-routes: `/general`, `/loan-products`, `/fees`, `/channels`, `/notifications`, `/users`, `/security`, `/system`
  - layout: 2-column (left nav `views/settings/_nav.ejs` + right content)
  - shared header partial: `views/settings/_header.ejs`

## Views
- `views/partials/header.ejs` — sidebar + topbar (รับ `page` เพื่อ highlight เมนู)
- `views/partials/footer.ejs`
- โฟลเดอร์ตาม module: `views/{loans,customers,payments,collaterals}/`

## Conventions
- ห้ามใช้ inline style ใน HTML ใหม่ (ของเดิมไม่ต้องแก้)
- ยกเว้นค่า style ที่ผูกกับข้อมูลจริง (สี, % progress) — ใช้ inline ได้
- ใช้ Sarabun font, สีและ badge ตาม pattern เดิม
- `breadcrumb`, `page`, `title` ส่งผ่าน `include('../partials/header', {...})`
- Status tabs: ใช้คลาส `.status-tabs > .status-tab` (responsive: pill บน mobile)
